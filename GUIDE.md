# DesignTool 用户指南

> 面向使用者的操作说明(界面内「📖 指南」页同步展示)。

## 快速开始

1. **登录/注册**:登录页可切换注册。注册默认「访客」权限,由管理员提升。
2. **新建项目**:左侧「设计」视图 →「+ New Project」。每个用户有独立数据目录(`data/projects/<用户名>/`)。
3. **开始设计**:对话输入框描述需求,例如:
   > 用 shell_box 做个盒子 100x80x60 壁厚5,生成STEP
   AI 会解析需求 → 建模 → 生成 STEP/STL。
4. **查看结果**:消息下方文件链接可 3D 预览(右侧抽屉可展开/收起/拖宽)、下载。
5. **多组件装配**:描述多个零件自动生成装配图;每轮结果独立目录,历史可追溯。

## 你输入的话发生了什么(工作原理)

你描述的每一句需求,都经过一条**确定性**链路变成模型:

1. **译码**:AI 查预注册的**译码表**(编号↔设计词汇对照表,如同摩斯电码本),把你的话译成一串数字——表里没有的词会触发 `9999` 报警而不是瞎猜("圆球"这类别名会先归一,"空心球"这类复合词会被拆解成基本体组合)
2. **校验**:数字串经文法校验,非法运算整体拒绝、定位到具体指令
3. **落账**:合法增量记入需求矩阵(Q′ = Q + ΔQ),修改不清空重建、任意步可回滚
4. **演算**:约束装配、求解、强度校核——**算**出唯一解,而非概率生成

这不是"AI 操作 CAD"。DesignTool 是一种**与 LLM 本质不同的机械智能**:AI 只负责听懂你的话,解题的是确定性内核——同一句话每次得到同一个模型;设计修改精确生效;每一步可追溯、可复算。技术细节见[README 工作原理](./README.md#工作原理llm-只译码不设计)。

## 角色与权限

| 角色 | 默认能力 |
|------|---------|
| 管理员 admin | 全部 + 管理入口(用户/授权/日志/用量) |
| 工程师 engineer | 设计对话/图元/甘特/标准件/OCR/语音 |
| 访客 viewer | 只读项目 + OCR/语音/草图/甘特 |

- 未授权工具:工具箱点「申请」→ 管理员审批后自动授权。
- 管理员可在「管理 → 工具授权」直接授予/回收(角色级/用户级)。

## 工具箱

- **✏️ 手绘草图**:绘图板 + 语音输入,AI 识别建模;语音兜底链路:浏览器识别 → whisper 免费转写。
- **🧩 图元库**:shell_box/u_channel/sphere/cylinder/subtract 等 17 原语。
- **📊 项目甘特图**:树形任务,父任务日期自动=子任务范围;后端持久化。
- **🔩 标准件查询**:标准/非标/行业/企业四表查询,策略选型。
- **🔍 OCR 识别**:本地 RapidOCR,图片/图纸转文字,免费。
- **🎤 语音输入**:浏览器 Web Speech + whisper 兜底(免费)。

## 模型配置(设置 → 模型配置)

用户可自设三类模型(留空用系统默认),仅自己账号生效:

- **推理模型**:设计对话/Agent(默认 DeepSeek)。
- **视觉模型**:手绘识别/图像理解(默认 Moonshot Vision)。
- **语音模型**:语音转写(未配置自动用免费 whisper)。

管理员可批量给测试用户统一配置(管理 → Token 消耗 → 批量配置模型 Key)。

## 数据共享

项目列表 🔗 按钮可共享项目给其他用户,三级权限:

- **可读 read**:查看结果/日志/文件。
- **可批注 comment**:读 + 写规则/甘特/评审。
- **可参与修改 edit**:读 + 写 + AI 生成建模。

共享项目出现在对方列表(带权限标记),只有所有者可删除/共享。

## 日志与追溯

- 每次设计对话、工具调用、模型构建都写入结构化日志(`.design/log`,JSON Lines)。
- 每次文件下载/预览记入下载追溯日志(`data/downloads.jsonl`)。
- 登录成功/失败记入登录日志(IP/UA)。
- LLM 调用 token 消耗记入 `token_usage`(管理 → Token 消耗)。
- 管理员可在「管理」统一查看。

## 服务架构(管理员参考)

| 服务 | 端口 | 说明 |
|------|------|------|
| nginx 前端 | 8091 | 静态资源 + API 代理 |
| Anvil 设计服务 | 8095 | AI 设计对话/建模/项目 |
| SketchService | 8096 | 手绘识别 |
| AdminService | 8097 | 认证/用户/授权/日志/用量 |
| VoiceService | 8098 | whisper 免费语音转写 |
| OcrService | 8099 | RapidOCR 免费文字识别 |
| MySQL | 3307 | anvil 库(users/roles/tools/shares/token_usage 等) |
