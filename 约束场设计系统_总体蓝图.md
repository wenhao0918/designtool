# 约束场设计系统 · 总体蓝图（发散输入组织稿）

> 版本: V0.1
> 日期: 2026-09-01
> 性质: **唯一总索引**。发散输入的组织收敛；细节见三份子文档，本文不重复展开。
> 文档地图: ①《技术框架规划.md》哲学与本体的上位源头
>          ②《设计语言演算架构_V0.md》文法/演算层/模块分工
>          ③《设计指令矩阵表示_V0.md》矩阵表示/意图三工具/字母表总表
>          ③.5《需求矩阵Q定义_V0.md》Q 正式定义：条目schema/七族双映射/翻译工效
>          ④ 本文——把以上全部串成一台机器
>          ★ 全域文档地图（含理论线/技术线档案）见同目录《导读.md》

---

## 一、范式命名与三条公理

> **用户前提（Premise，2026-09-02 定调）**：本工具**面向业内专业人士**（机械设计工程师/结构专家），非消费级产品。
> 这不是市场描述，是**系统成立的逻辑前提**——四决策模式、术语体系、专业回显、责任模型全部构建在此假设之上；离开此前提，系统保证不适用。交互假设用户具备专业素养（术语/图纸/公差/工艺），能执行专家裁决与托管模式；
> 回显、错误信息、文档均使用专业语言，不做消费级简化。
> 责任模型：**专家拍板，工具放大**——专业人士的每一次裁决都是设计的一部分并被记录。

**模型范式身份：强约束AI模型（Strongly-Constrained AI）——区别于 LLM 的第二类 AI 模型。**
Primordium（元初）是它的框架实现；与 LLM 是 STACK 关系而非竞争：LLM 管语言的概率世界（翻译官），
强约束AI 管物理的必然世界（引擎）——依据"物理允不允许"，求"违例即死"，不问"概率最大"。

**AI 属性四支柱（新范式AI 的"新"字依据）**：
① 语言智能——Anvil LLM 翻译（神经/归纳）
② 推理智能——文法校验、约束判定、零空间诊断（符号/演绎）
③ 规划智能——Π 任务分解+异构标注（AI Planning 正统问题）
④ 进化智能——Ω 原语注册自进化、翻译数据飞轮、EG 权重沉淀、设计空间批量搜索（学习/累积）

定位语：AI 第一代（专家系统）严谨但不会说话；第二代（LLM）会说话但不严谨不可复算；
**新范式AI = 语言能力 + 演绎严谨 + 可验证可信**——学术称 neuro-symbolic，我们称强约束AI：
会说话的裁决场。OS 类比只描述内核的工程形态；系统的智能身份是**新范式AI**。

**范式：需求矩阵 → 结构求解（Q → S）——求解式设计。**（需求矩阵记号 Q；设计指令 = ΔQ，Q′ = Q + ΔQ）

### 一.5 两项红利：站在 LLM 与 NPU 的肩膀上

| | 红利 | 我们的用法 | 没有它 |
|---|---|---|---|
| 语言侧 | **LLM 红利**——全行业训练出的语义能力 | 只做调用：用户话→ΔQ 的翻译官，免费档（glm-4.5-flash）即够 | 得自建 NLP |
| 算力侧 | **NPU 红利**——国产 NPU 生态成熟的廉价张量算力 | 只做征用：批量求解/场验证的肌肉 | 大规模探索算不动 |

**两项红利都不提供的：约束求解本身（物理必然性）——这才是强约束AI 的内核资产。**

### 一.5.5 应用定性：研究 / 研发 / 产品设计 / 机械DIY（四层光谱）

| 层 | 用户 | 用什么 | 严格度 |
|---|---|---|---|
| **研究** | 理论/学术界/我们自己 | 公理体系、矩阵表示、Q→S 范式、新硬件愿景 | 理论证明级 |
| **研发** | 工程团队 + 生态开发者 | 内核四件套、Ω 原语库、复合原语注册 | 代码级（测试/回放校验/漂移监控） |
| **产品设计** | **专业机械工程师（核心用户，用户前提全量生效）** | 完整 Q→S：ΔQ/验算/不可达判定/二值律/WYSIWYG/交付解 | **工程级：强约束二值律强制** |
| **机械DIY（C端，不排除且认真做）** | 创客/爱好者/学生/泛消费者 | **①动画输出**：机构运动动画（J 副矩阵驱动）、装配过程动画、设计生长回放（DemoView 已有雏形），导出 mp4/GIF 分享 ②**游戏化交互**：拖拽原语/术语块拼搭，约束引擎实时校验（拼错即提示——强约束的游戏化）、关卡化机械谜题 ③**实物出口**：验算通过 → STL → 3D 打印，虚拟创作变实物 | 爱好级（预览级判定 + 实时提示；打印导出走验算） |

**梯度规则**：二值律在产品设计层强制；DIY 层运行预览级判定（VIOLATION 仍拦截，验算/交付解按需开启）——严格度随层递减，但**判定的诚实性不递减**（任何层都不出"假 PASS"）。用户前提在研究/研发/产品设计层全量生效；DIY 是前提的放宽延伸，也是 EG 具名块与飞轮数据的最大众来源。

### 一.6 远期愿景：从征用红利到催生新硬件

硬件永远跟着杀手级负载走（游戏→GPU，LLM→NPU）。求解式设计若成功，即定义一个持续规模化的新负载，其形态与 LLM 芯片处处相反——**稀疏**（非稠密）、**精确可验证**（非低精度采样）、**海量小任务批量**（非单一巨任务）、**DAG 数据流**（非均匀流水）、**秒级交互**（非离线吞吐）。

由此倒推"约束求解处理器"四个硬件特征：稀疏求解引擎（不规则稀疏友好）、批量小核阵列（万路小解并行）、区间运算单元（硬件级误差界 → 判定带数学证明的可验证计算）、DAG 数据流调度。

> 设计不是画出来的，也不是生成出来的——**设计是需求方程组的解**。
> 同样需求必得同样结构；需求不合理（欠定/冲突）是系统的合法输出，不是垃圾进垃圾出。

对照生成式路线：采样→每次不同 vs 求解→可复算；模型替你猜 vs 零空间报自由度；
静默糊过 vs 残差报冲突。设计 = 需求系统的稳态解（上位文档"稳态解"的工程落地）。

**一句话愿景**：用户说需求，系统算出制造可得的三维模型——每一步可追溯、可判定、可复算。

**需求矩阵 Q** = 五套矩阵统称（几何 I Θ F S B ／物理 M̂ L E ／运动 J ／工艺 Cₘ ／意图三工具）。
**结构求解 sol(Q)** = 稀疏装配 + 零空间诊断 + CSG 演算 + PASS/VIOLATION 门禁。

| # | 公理 | 来源 | 数学载体 |
|---|---|---|---|
| 1 | **结果是算出来的**——演绎替代归纳，建模决策不放在 LLM 里 | 2026-09-01 事故定调 | 定义+算子+方程的演算 |
| 2 | **词汇可穷举，组合无限**——文法封闭 | 字母表定调 | 有限字母表上的形式语言 |
| 3 | **设计的自由度 = 约束矩阵的零空间**——设计=零空间受控收缩 | 矩阵化推论 | ker(A) 的秩诊断 |
| ★ | **同 Q 必得同 S（计算稳定性）**——公理 1 的可判定形式与一票否决条款：不满足则"算出来的"不成立，系统不成立。稳定性缺陷 = 最高优先级缺陷，验证指标见结构定义 §三.5 五层保证 | 定调 2026-09-02 | 纯函数内核/两级一致性/路径无关/确定性收敛/漂移监控 |

---

## 二、整机链路（从一句话到 STEP）

层级定调：**前端 → 知音 → Anvil → Primordium（唯一框架层：Q/S 账本 + ΣΠΔΩ 内核，Ω 执行层在此）→ 工具载体（PrimitiveService 8103 / CADService 8102）**。

```
用户话（自然语言，无限）
   │ ⓪ 前端【交互层】草图 / 回显确认 / 设计日志 / 3D查看 / 欠定自由度交用户决策
   ▼
⓪.5 知音【语义归一层】方言/俗称/行话 → 标准术语上下文（四级漏斗：词表精确→MySQL术语→RAG 四层表达→图谱消歧；LLM 只见归一化后的词）
   ▼
① Anvil【应用层·壳层翻译官】LLM 只翻译不做建模决策
   │  模型策略：每人自配推理/视觉模型；测试用户用默认；翻译不需强推理 → glm-4.5-flash 免费档
   ▼
设计语言句子（可穷举字母表上的形式语句；术语层：行话→具名块确定性展开）
   ▼
② Primordium【框架层】（载体 = PrimitiveService 8103，MCP/HTTP）
   │  五套矩阵（几何 I Θ F S B｜物理 M̂ L E｜运动 J｜工艺 Cₘ｜意图三工具｜组织 G｜溯源 Φsrc）
   │  grammar 文法校验 → resolver 矩阵演算（FEM式装配 A=ΣLⱼᵀAⱼLⱼ → 稀疏解 + 零空间诊断）
   │  CSG 代数层（形状 = 定义+算子） → constraints 判定（𝒫/𝒞ₘ → PASS/VIOLATION）
   ▼
③ CADService【FreeCAD 工具载体】8102 → STEP/STL（执行层在 Primordium Ω；载体可替换，框架不受影响）
   ▼
模型 + 全程演算轨迹（每个坐标可回答"哪个方程、哪句指令"）
```

**MCP 调用关系**：LLM 唯一建模动作 = 调用 Primordium 计算工具（design_sentence → 8103 → 8102）。层层是工具，层层可独立测试。框架（Primordium）与载体（PrimitiveService 服务）分离——框架可复用，载体可替换。

---

## 二.5 NPU 征用点地图（需求/已知结构/结果皆矩阵 → 张量主场）

需求 R、已知结构（标准件库/术语块/历史设计）、结果 S 全部矩阵化后，NPU 直接征用，确定性一分不失：

| 环节 | 矩阵运算 | 算力 | 阶段 |
|---|---|---|---|
| 装配 A=ΣLⱼᵀAⱼLⱼ | 稀散射/索引管理 | CPU（符号图操作） | V1 |
| 位姿求解 | 稀疏解；CG 内环(A·x,dot) | CPU → CG 体 NPU | V1/V3 |
| 设计空间探索 | 同稀疏模式批量右端 A·X=B(Θ) | **NPU 甜点** | V2 |
| 物理场验证 | K·u=f 共轭梯度 | **NPU**（5x 收敛指标） | V3 |
| 干涉粗检 | 包围盒距离批量算 | NPU | V2 |
| 结构复用 | 标准件库/术语块=矩阵块,复用=块替换+重解 | 块检索向量化 NPU | V2 |
| 相似设计建议 | (R_i,S_i) 语料 embedding 检索 | NPU——**只出建议,落 𝒞ᵥ 层** | V2 |

**纪律**：征用边界画在判定权上——解可算在 NPU，PASS/VIOLATION 永远由 CPU 符号层不等式判定。NPU 提速，不裁决（铁律 3：纯确定性加速器，非自适应控制器）。

彩蛋：已知结构矩阵化 ⇒ 国标库(GbstdService)升级为**矩阵块库**，装配从"建模"退化为"选块+解算"。

---

## 三、理论支柱（四根，各对应一个数学对象）

| 支柱 | 数学对象 | 回答的问题 |
|---|---|---|
| 文法封闭性 | 有限字母表上的形式语言 | 无限的用户话怎么被确定性地接住 |
| 零空间理论 | ker(A)：设计自由度 | 什么算"设计完成/欠/冲突"；机构=故意留下的零空间；意图=被保护的方向；**意图漂移=保护方向被吞（可判定）** |
| 演算代数 | CSG 算子（壳/布尔/阵列）＋**算法原子化** | 形状从哪来（30 是 50−20 减出来的）；算法一律分解为计算原子，复杂算法=原子嵌套 |
| 所见即所得 | 预览解随 ΔQ 自动更新（增量编译+快速求解），交付解仅 S4 精确构建 | 任何时刻前端可预览，不等定稿；欠定占位高亮不猜不空白 |
| 可追溯性 | Φsrc（元素→指令号） | 每个几何特征凭什么存在 |

---

## 四、模块分工（含禁止事项）

| 模块 | 职责 | 禁止 |
|---|---|---|
| Anvil Agent(LLM) | 用户话→句子；调 MCP 计算工具；回述结果与 VIOLATION | 选原语、算坐标、建模方法、VIOLATION 后自行换方案 |
| 术语层(词典) | 行话→具名块展开 | 产生文法外结构 |
| grammar 文法校验 | 词汇∈表/结构合文法/参数完备 | 做几何计算 |
| resolver 矩阵演算器 | 装配 A、稀疏解、零空间诊断、CSG 形状演算 | 调 LLM；含设计决策分支 |
| constraints 判定 | 𝒫/𝒞ₘ 不等式 | 输出置信度/建议 |
| CADService | FreeCAD 执行，出 STEP/STL | 理解语义 |
| 用户 | 歧义回显确认；欠定自由度决策 | 被（系统/LLM）代猜 |

---

## 五、模型策略层（与架构改革是同一件事）

为什么"LLM 只是翻译、不需强推理"成立：**系统的全部智能已经从概率侧（LLM 权重）搬到了确定性侧（文法+矩阵+算子）**。翻译是查词典+填参数——glm-4.5-flash 免费档足够；强推理的活儿根本不存在于它的岗位说明书里。

今日已上线：users.is_test 三策略（自配/测试默认/强制自配）、每用户模型配置全链路、模型误配事故防护。

---

## 五.5 翻译质量保障体系（四道防线）

"如何保证翻译对"——不压在 LLM 身上，组织为四道防线：

| 防线 | 手段 | 性质 |
|---|---|---|
| ① 输入侧 | 知音语义归一四级漏斗（词表精确→术语库→四层表达向量→图谱消歧，见《语义数值映射定义_V0.md》§七）；few-shot 例句库；分级重试（免费模型→校验失败→强模型×1→回显问用户）；相似历史指令句对复用 | 提高一次译对率 |
| ② 结构侧 | 文法校验打回；结构化回显确认（人审一道）；演算层可证伪（VIOLATION/欠定/干涉暴露错译） | **错误必然被捕获，不逃逸** |
| ③ 度量侧 | 金测试集（指令→标准句子对+数值验收点），每次改 prompt/换模型回归跑分 | 准确率是数字不是感觉 |
| ④ 沉淀侧 | 用户纠正=(指令,正确句子)对→回流 few-shot/RAG 库 | 质量飞轮 |

关键推论：这是"结果是算出来的"在翻译层的投影——**翻译正确率也是算出来、测出来的**。
金测试集 v0 起点就是今日真实用例（空心球三指令）。

---

## 六、演进路线（合并版）

| 阶段 | 内容 | 里程碑验收 |
|---|---|---|
| **V0 ✅** | 文法+演算原型（3体元/1算子/1关系），翻译→计算分工确立 | 空心球精确解 ✅ |
| **V1** | 矩阵求解器：五张几何矩阵+稀疏装配+spsolve+零空间诊断；术语块词典 v0（8~15 条高频）；旧闭式解退役；**所见即所得**（ΔQ 落账自动预览解，欠定占位高亮） | 删锚定行→报"欠定"；术语"盲孔"展开正确；任一指令后前端秒级见形；**同 Q 复算位级一致、回放漂移=0（计算稳定性）** |
| **V1.5** | 意图三工具入文法；运动副 J；制造 Cₘ | 调义被吞→INTENT_LOST；四杆机构 M=1 算对 |
| **V2** | 物理块（材料/载荷/工况+5~8 个角色单位验证器）+ 价值 w；**评审矩阵 RV**（ReviewManager 矩阵化）＋**专家指导矩阵 EG**（mech_terms/GbstdService 整合，NPU 相似检索） | 500kg 静载 PASS/VIOLATION；RV 评审报告出 issues 并回流 ΔQ |
| **V3** | 场离散化 K·u=f，CG 卸载 NPU（对齐上位文档 V2/V3 指标） | 批量参数扫描加速比 |

---

## 八、待拍板清单（汇总，按阻塞排序）

1. **V1 是否开工**（矩阵求解器替换闭式解）——本蓝图最大的施工决策
2. 意图词表：调义/固义的用户话约定
3. 物理验证器清单（建议首版：板承重/轴扭/销剪/梁弯/压杆稳定）
4. 术语块首批清单（建议：盲孔/通孔/沉孔/法兰盘/键槽/加强筋/提手）
5. 旋转自由度(SE(3))引入时机
6. 角色模板库归口（建议随 GbstdService 国标库走）
7. 今晚遗留：LLM 翻译错误日志排查（并入 V1 验收用例）
8. （DP 评审采纳）CPU/NPU 双忙或降级超时预案：人工介入/搁置队列机制

