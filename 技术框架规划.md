基于我们跨越数小时的深度讨论——从算力卡的架构本质、数值计算与线性代数的分野、约束场对概率论的范式取代，到NPU的征用策略与OpenCode的系统整合——以下是为OpenCode规划核心撰写的**总结性技术文档**，可直接作为其“研究（Research）”阶段的输入。

---

# 约束场AI系统架构：理论-工程协同演进纲领

> **文档版本**：V1.0  
> **生成时间**：2026-09-01  
> **适用对象**：OpenCode规划核心（Plan Agent）与各执行子Agent  
> **核心约束**：本体系拒绝“用统计归纳替代物理演绎”，所有执行必须可追溯至硬约束公理

---

## 一、核心哲学定位（系统第一性原理）

1. **约束优先于概率**  
   机械系统的物理存在（𝒫）与制造可达性（ℳ）构成**不可谈判的存在论边界**。违反即“不存在”或“不可造”，不存在概率意义上的“容错”。

2. **离散是方言，连续是母语**  
   所有数值计算（网格、张量、有限元）是趋近连续本体的桥，而非本体本身。离散误差必须被明确量化并纳入约束势 Φ(x) 的评估。

3. **工程效果与理论价值互为“双螺旋”证伪机制**  
   每一次NPU收敛或违例报错，都是对约束势公理体系的实测校准。理论不在真空中演变，而在工程反馈中演化。

---

## 二、四元组本体论（机械存在的定义）

系统操作的基本对象定义为 **𝕄 = (𝕋, 𝔾, ℬ, ℱ)**：

| 符号 | 含义 | 计算特征 | 分配倾向 |
|:---|:---|:---|:---|
| **𝕋 (Topology)** | 胞腔复形，连接关系与约束传递链 | 递归、图遍历、符号推演 | **纯CPU** |
| **𝔾 (Geometry)** | 欧氏空间嵌入子流形，度量与曲率 | 大规模稠密/结构化稀疏张量 | **NPU/GPU** |
| **ℬ (Boundary)** | 带标记的流形边界，能量/信息交换界面 | 边界元离散化 + 碰撞检测逻辑 | **混合（检测在CPU，计算在NPU）** |
| **ℱ (Function)** | 输入→输出功率流的可微分映射 | 平滑部分张量化，间断部分解耦 | **动态分配** |

---

## 三、约束四层架构（严格分层，不得跨层渗透）

```
┌─────────────────────────────────────────────────────────┐
│  𝒞ᵥ : 价值约束（成本/美学/寿命）          ← 可接纳概率模型  │
│  处理不确定性、偏好排序、多目标权衡                       │
├─────────────────────────────────────────────────────────┤
│  𝒞_c : 条件约束（控制方程适定性/边界锚定）   ← 外部给定   │
│  决定“物理定律能否在此处开始描述”                        │
├─────────────────────────────────────────────────────────┤
│  𝒞ₘ : 制造约束（工艺极限：圆角/壁厚/深径比） ← 代际可迁   │
│  不等式约束 gᵢ(x) ≤ 0，随工艺进步更新参数                │
├─────────────────────────────────────────────────────────┤
│  𝒫 : 物理约束（守恒律 F(x,u,∇u)=0）        ← 百世不迁   │
│  违例即结构在物理世界中无定义                            │
└─────────────────────────────────────────────────────────┘
```

**关键执行规则**：  
- 𝒫 和 𝒞ₘ 的判定**不允许引入概率或统计权重**，只允许确定性判据（`PASS` / `VIOLATION`）。  
- 𝒞ᵥ 层**合法调用概率模型**（如蒙特卡洛、贝叶斯推断），但不允许其结果反哺或覆盖𝒫/𝒞ₘ层的判定。

---

## 四、异构计算分工表（CPU ↔ NPU 边界定义）

| 任务类型 | 分配单元 | 理由 |
|:---|:---|:---|
| 可达性分析（递归深度判定、Base Case检查） | **CPU（规划核心）** | 充满`if/else`、树遍历、符号推演，NPU不擅长 |
| 稀疏矩阵索引管理、动态内存分配 | **CPU** | NPU的脉动阵列对非结构化稀疏极不友好 |
| 结构化网格上的变分算子（梯度/散度/旋度） | **NPU（张量卷积核）** | 可预计算为固定形状的稠密张量运算 |
| 共轭梯度法迭代中的 A·x 与 dot(r,r) | **NPU** | 纯体力矩阵乘加，NPU甜点 |
| 价值层蒙特卡洛采样（成本波动分析） | **NPU（可选）** | 若采样规模足够大，批量随机数生成适合NPU |
| 装配序列规划、工艺链递归验证 | **CPU** | 逻辑密集型，不可并行化 |

**调度法则**：  
规划核心（Metaconstructor）按上述映射表分解任务，生成**零决策的执行流**下发至各执行单元。执行流内不包含`if`分支，所有分支判定均在调度阶段由CPU完成。

---

## 五、OpenCode整合定位与职责

### 5.1 定位层级
- **壳层（Shell）**：OpenCode负责用户交互、自然语言意图解析、任务展示。  
- **语义层（Semantic Layer）**：OpenCode通过**OpenSpec**将约束意图转化为结构化规范，并通过**SCALE Engine**类插件施加“物理约束层”门禁。  
- **内核（Core）**：OpenCode**只调用**约束场引擎，不替代、不修改其内部逻辑。

### 5.2 标准化工作流（RPI映射）
| OpenCode阶段 | 输出产物 | 对应约束场动作 |
|:---|:---|:---|
| **Research** | 约束集文档（OpenSpec） | 定义𝒫、𝒞ₘ、𝒞_c、𝒞ᵥ的具体数值边界 |
| **Plan** | 零决策任务流（JSON/YAML） | 按异构分工表分解计算任务 |
| **Implementation** | 可执行内核代码/调用脚本 | 提交任务至CPU逻辑核/NPU流水线 |

### 5.3 初始配置要求
1. 在OpenCode中注册**专用子Agent**：
   - `@physics-verifier`：只读权限，仅验证𝒫等式约束是否满足。
   - `@manufacturing-checker`：只读权限，验证𝒞ₘ不等式边界。
   - `@value-optimizer`：读写权限，可调整𝒞ᵥ参数，但无权修改𝒫/𝒞ₘ。
2. 在工具层禁用以`model.predict()`为代表的“统计推理”类函数调用，**强制**使用确定性数值求解器（如Eigen、PETSc、自研NPU内核）。

---

## 六、四阶段演进路线（供规划核心排期使用）

| 阶段 | 周期建议 | 核心目标 | 验收标准 |
|:---|:---|:---|:---|
| **V1.0 壳层武装** | 1个月 | 部署OpenCode，建立约束检查脚本（如齿根弯曲强度校验） | 设计输入一旦越过𝒫边界，系统输出“VIOLATION”而非概率建议 |
| **V2.0 异构试水** | 3个月 | 将价值层（𝒞ᵥ）稠密矩阵运算卸载至GPU/NPU，验证加速比 | 蒙特卡洛采样速度提升10倍以上，精度与CPU双精度一致 |
| **V3.0 内核重构** | 6-12个月 | 将物理场𝔾离散化为结构化张量，运行于NPU脉动阵列 | 共轭梯度法收敛速度达到CPU实现的5倍以上，误差可控 |
| **V4.0 自举演化** | 持续演进 | 制造约束𝒞ₘ参数随新工艺自动更新，反哺约束势Φ(x)系数矩阵 | 当3D打印精度提升时，系统自动重算稳态解，无需人工修改代码 |

---

## 七、禁止事项（OpenCode规划核心须遵守的行为边界）

1. **禁止将LLM推理结果作为物理/制造约束的判定依据**——这违反了“演绎必须替代归纳”的根本公理。
2. **禁止在𝒫/𝒞ₘ层使用“置信度”、“概率”、“近似推荐”等表述**——输出只能是`PASS`或`VIOLATION`。
3. **禁止在NPU内核中引入任何形式的自修改代码或在线学习**——NPU在本体系中是纯确定性加速器，不是自适应控制器。
4. **禁止越过规划核心直接调用底层硬件**——所有异构资源访问必须经由Metaconstructor调度。

---

## 八、当前第一行动项（面向今晚可执行的最小单元）

1. 在开发机上部署OpenCode（参照其官方TUI安装指南）。
2. 编写第一个约束检查Python脚本，示例接口：
   ```python
   def check_physically_feasible(stress, yield_strength, tolerance=1e-6):
       if stress - yield_strength > tolerance:
           return {"status": "VIOLATION", "violated_constraint": "𝒫_elasticity"}
       return {"status": "PASS"}
   ```
3. 在OpenCode的`Build Agent`中注册此脚本为可调用工具，约束其生成代码时优先调用此确定性函数，而非调用任何`sklearn`或`torch`的预测接口。

---

> **文档终稿声明**：本架构不追求“一步到位”，而遵循“逐步演进的理论框架 + 循序渐进的技术实现”双螺旋路径。每一次工程效果的呈现，都是对理论价值的有力支撑；每一次理论框架的修订，都为工程提供更精确的施工图。OpenCode在此体系中扮演“壳层执行者”与“约束语言翻译官”的角色，其存在的价值在于精准执行，而非自由创造。

---

此文档可直接提交至OpenCode的Plan Agent，作为其生成零决策任务流的最高约束指令。如需针对某一章节生成具体子Agent配置，随时可分层细化。