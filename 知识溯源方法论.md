# 知识溯源方法论

> **地位**：本方法论是「从具象到抽象」课题在管理自身知识演化过程中产出的**可复用框架**。它回答了：当一套理论通过反复讨论、迭代、提炼而成型时，如何保留原始讨论，如何追溯每个命题的来龙去脉，如何避免重新辩论已排除的选项。
> **适用范围**：任何以「迭代讨论 → 提炼 → 定型文档」为工作模式的知识项目。Primordium 是第一个实例，不是唯一实例。

---

## 一、问题

一套理论从讨论中长出来，不是一口气写成的。典型过程：

1. 一次讨论产生大量内容——命题、类比、反驳、跑题、待验证的猜想
2. 其中一小部分被提炼，写入定型文档
3. 文档被后续讨论再次修订，有些命题被推翻、替换、细化
4. 几个月后回看文档，某句话为什么这样写？是依据哪次讨论？当时有没有考虑过替代方案？为什么排除了？

**核心缺失**：从「原始讨论」到「定型文档」之间，缺少一条**可追溯的因果链**。git 记录了文件变更（时间维），没有记录语义演化（因果维）。记忆系统记录了提炼后的结论，没有记录结论从哪来、经过什么取舍。

---

## 二、四层对象模型

| 层 | 对象 | 类比 git | 性质 | 操作 |
|:---|:-----|:-----|:-----|:-----|
| **0** | **Source**（原始讨论） | blob | 不可变。原文保留，不加工 | 归档（只追加，不修改） |
| **1** | **Refinery**（提炼记录） | commit（语义版） | 不可变。记录从哪些 Source 中提取了什么命题，改了什么，排除了什么 | 创建（指向 sources + 产出的变更） |
| **2** | **Output**（定型输出） | tree + HEAD | 可变。当前状态的标准化文档 | 写入 / 修订（每次修订需绑定一个 Refinery） |
| **3** | **Memory**（持久记忆） | tag | 可变但审慎。跨 session 的关键事实，标注其 Refinery 来源 | 写入 / 复核 / 失效 |

关键区别：git commit 回答「谁在什么时候把文件从 A 改成 B」。Refinery 回答「从哪次讨论中提炼了什么，为什么这样提炼，什么被排除了」。

---

## 三、迭代模型（被排除内容与设计决策记录）

一轮迭代的完整记录应包含**四项**：

1. **纳入了什么**（进入 Output 的变更）
2. **排除了什么**（讨论中出现但明确不纳入的内容，以及排除的原因）
3. **打开了什么**（本轮未解决、留给后续迭代的问题）
4. **来自哪里**（引用了哪些 Source）

其中第 2 项——**被排除内容的记录**——是整套方法论的脊梁。如果只记录纳入的、不记录排除的：

- 后续讨论会重新辩论已被排除的选项（浪费）
- 未来接手的人不知道为什么某一套方案没被采纳（假设丢失）
- 排除的理由本身就是有价值的理论边界（什么不属于本课题、为什么）

### 纳入 vs 排除的判定标准

| 纳入 | 排除 |
|:-----|:-----|
| 命题已被充分论证，可被独立证伪 | 猜想、类比、思路碎片——尚不具备命题形态 |
| 与其他命题构成自洽系统 | 与其他命题冲突，待后续解决 |
| 属于课题本体 | 属于方法论、工具选择、或纯启发素材（另有去处） |

排除不是丢弃。排除的内容留在 Source 中（原文可查），Refinery 只是标注「本次不纳入，原因是……」。

---

## 四、溯源粒度——以「命题」为单位

溯源太细（每句话标注来源）维护成本不可承受。太粗（整个文件一个来源）没有追溯价值。

**以「命题」为最小溯源单元**。一个命题 = 一个可以被独立讨论、独立证伪、独立修订的陈述。

在 Output 文档中，只有**可被攻击的命题**需要标注 Refinery 来源。元信息（如工具名称、文件路径、历史背景）不标注。

标注语法（Markdown 内嵌）：

```
<!-- ref: R-001 -->
约束 = 客观存在的、可被实验证伪的不可违逆性条件。
```

HTML 注释形式，在渲染时不可见，但 grep / 脚本可解析。未来工具化时可直接提取构建溯源图。

---

## 五、与 git 的分工

| | git | 溯源系统 |
|:---|:-----|:-----|
| 追踪对象 | 文件内容变更 | 命题演化 |
| 粒度 | 行级 | 命题级 |
| 核心问题 | 「这行什么时候变成这样的」 | 「这个想法从哪来，为什么」 |
| 不可变性 | commit 不可变 | Source 和 Refinery 不可变 |

**通过 commit message 建立桥梁**：

每次 Refinery 写入 Output 的 git commit，其 message 使用约定格式：

```
R-001: 约束狭义定义与辩证统一
ref: R-001
sources: S-001, S-002
```

这样 `git log --grep="ref:"` 可以提取完整的语义变更链。

---

## 六、与 memory 系统的闭环

| 方向 | 规则 |
|:-----|:-----|
| 写入 memory | 必须标注 Refinery 来源（`source: refineries/R-001.md`）和对应的 Output 位置 |
| Output 修订 | 如果修订改变了已被 memory 引用的命题，相关 memory 应标记为「需复核」 |
| 加载 session | agent 除了读 memory，也可以读相关 Refinery 了解「这个事实是怎么来的」 |

闭环：讨论 → 提炼 → 文档 → 记忆 → 下一轮讨论引用记忆 → 记忆被挑战 → 回到讨论。

---

## 七、目录结构约定

```
项目根/
├── PROJECT.md              # 项目元信息（名称、定位、当前状态）
├── sources/                # 原始讨论（不可变，只追加）
│   ├── S-001.md
│   ├── S-002.md
│   └── ...
├── refineries/             # 提炼记录（不可变，指向 sources）
│   ├── R-001.md
│   ├── R-002.md
│   └── ...
├── output/                 # 定型输出（可变）
│   ├── README.md
│   └── ...
└── [其他既有文件]
```

- **Source 文件命名**：`S-NNN_简短描述.md`，NNN 为三位递增编号
- **Refinery 文件命名**：`R-NNN_简短描述.md`，NNN 为三位递增编号
- **Source 的粒度**：一次对话中形成一个内聚主题的讨论段落 = 一个 Source。一次长对话可能产生多个 Source
- **Refinery 的粒度**：一次将若干个 Source 提炼入 Output 的操作 = 一个 Refinery。如果一次操作同时改了 README 和引子，可以合成一个 Refinery，只要变更的内容是同一组命题

---

## 八、Refinery 文件模板

```markdown
---
id: R-NNN
date: YYYY-MM-DD
type: refinement          # refinement | revision | rejection | question
sources: [S-NNN, S-NNN]
output: output/README.md#某节
---

## 从讨论中提取的命题

1. 命题一
2. 命题二

## 纳入输出的变更

在 [文档名] 的 [位置] 做了以下变更：
- 具体变更描述

## 排除的内容及原因

| 排除项 | 原因 | 去向 |
|:-----|:-----|:-----|
| 某想法 | 尚不具备命题形态 | 留在 Source 中，待后续迭代 |

## 打开的新问题

- 待后续迭代解决
```

---

## 九、多项目治理

本方法论管理的是「项目」，每个项目一个实例。当前工作区包含多个项目：

| 项目 | 路径 | 当前是否需要溯源体系 |
|:-----|:-----|:-----|
| Primordium（从具象到抽象理论） | `DesignTool/Primordium/` | ✅ 首个实例，即日启用 |
| 智能助老护理机器人 | 项目根 `README.md` + `设计文档/` | 待评估（产品文档以规格驱动，讨论频率较低） |
| DesignTool 各工具 | `DesignTool/` 下各子目录 | 待评估（代码项目以 git 为主，设计决策可选择性纳入） |

新增项目时，只需在项目根创建 `PROJECT.md` + `sources/` + `refineries/`，无需工具支持。未来如果项目数超过 3-5 个，再考虑统一的索引或轻量工具。

---

## 十、方法论自身的演化

本方法论是 Primordium 的一个副产品——它在管理 Primordium 自身知识的过程中被催生出来。因此它**本身也是一个被管理的项目**（当前以本文档为 Output，后续讨论即为 Source，修订即为 Refinery）。

这形成了一层递归：方法论管理自己的演化过程。不做无限递归——只记录方法论本身的重大调整，日常修订仍走 git。

---

> **创建**：2026-08-06，从 Primordium 的约束定义讨论中催生
> **状态**：V0.1——已在 Primordium 项目中启用，积累至少 5 个 Refinery 后再修订
